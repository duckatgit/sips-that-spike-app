import nodemailer from 'nodemailer';
import dotenv from 'dotenv';
dotenv.config();


// const userPass = async () => {
//   let getEnvCred = await emailTempelateServices.getEnvCredById()
//   // assuming only one record in DB
//   let creds = getEnvCred[0] || {};

//   return {
//     EMAIL_USER: creds.EMAIL_USER,
//     EMAIL_PASS: creds.EMAIL_PASS,
//   };
// };




export default {
  // sendOtpEmail
};

